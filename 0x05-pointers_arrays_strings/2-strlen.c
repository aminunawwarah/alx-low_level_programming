#include "main.h"

/**
*_stl_len -> is the main function
*@s: is the formal parameter of the function
*Return: returns an int
*/
int _strlen(char *s)
{
	int length;

	for (; *s != '\0'; s++)
	{
		length =+ 1;
	}
	return (length);
}
